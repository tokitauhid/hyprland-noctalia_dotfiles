#STARTUP

# Autostart apps to the system tray
exec-once = uwsm-app -- steam -silent
#exec-once = uwsm-app -- kdeconnect-indicator

# Autostart Discord minimized
exec-once = uwsm-app -- discord

exec-once = qs -c noctalia-shell  
exec-once = hyprctl reload &

#ENVIRONMENT VARIABLES
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland

#Nvidia (Uncomment if using Nvidia GPU)
#env = WLR_NO_HARDWARE_CURSORS,1
#env = LIBVA_DRIVER_NAME,nvidia
#env = GBM_BACKEND,nvidia-drm
#env = __GLX_VENDOR_LIBRARY_NAME,nvidia
#env = XDG_CURRENT_DESKTOP,Hyprland
#env = XDG_SESSION_TYPE,wayland
#env = NVD_BACKEND,direct
#exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
#exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

#QT 
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1

#ELECTRON
env = ELECTRON_OZONE_PLATFORM_HINT,wayland


#EXEC-ONCE SERVICES

#Polkit (required)
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 || /usr/libexec/polkit-gnome-authentication-agent-1

#GNOME Keyring (optional i.e Browser Passwords)
#exec-once = gnome-keyring-daemon --start --components=secrets

#Idle daemon
exec-once = hypridle &

#OverView Mode
exec-once = hyprpm reload -n
